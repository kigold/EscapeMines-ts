import {TurtleStatusEnum, DirectionEnum, BoardTileEnum, MovesEnum} from "./Enums";
import {Run} from "./Run";
import LocationData from "./Data/LocationData";

Run();
/*var a:LocationData[];
var t = "L M M M R R M"
const arr = t.split(' ').map(x => MovesEnum[x]);
arr.forEach(element => {
    var temp = element +1
    //console.log(MovesEnum[element] + " => " + element)
    console.log(MovesEnum[temp] + " =>Temp " + temp)
});*/

